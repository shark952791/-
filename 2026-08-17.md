# 2026-08-17 工作记录

## 槐安庙线上服务平台（恐怖互动游戏 · 第一阶段原型）
- 交付物：纯前端网站原型，位于 `C:\Users\sh\WorkBuddy\2026-08-17-17-28-20\`
  - `index.html` / `css/style.css` / `js/app.js`
  - 含页面：首页、在线预约、还愿服务、祈福服务、庙宇介绍(历史)、周边商城、联系我们、在线客服、404
- 产品背景：Pocket World 项目下一个恐怖互动网页游戏的第一阶段。玩家第一阶段看到的必须是「真实存在过的传统民俗庙宇官网」，节奏土味/复古/红金/早期中文互联网风。
- 第一阶段硬约束：绝不出现鬼、血、尸体、恐怖脸、祠堂、邪神、献祭、诅咒、死亡、灵异等恐怖元素；不要解释「槐安庙」真实设定。
- 关键设计决策：
  - 所有可变内容/话术抽到 `HUAIAN.config`（公告、历史、商城、客服话术）。
  - 钩子系统 `HUAIAN.on/emit`（pageView / reserveSubmit / csMessage / urlParam / boot），为后续「污染」剧情预留扩展点。
  - LocalStorage 保存进度（reservations/repayments/prayers/feedback/pageLog/flags）。
  - hash 路由，可正常跳转、支持返回；URL 参数读取（?ref=）。
- 后续阶段：逐步用 config + hooks 注入异常文本、客服矛盾回答、隐藏页面、不可靠叙事等「污染」内容。
- 验证状态：JS 语法通过 `node --check`，HTML 内所有脚本引用 ID 均存在。未做浏览器实渲染截图（环境无 HTML 截图工具）。

## 视觉修订（第四轮 → 第五轮 · 同日）：回正 —— banner 用实拍庙宇做底图，标题/副标/按钮全部回到 HTML overlay 层
- 第四轮我误把用户给的 banner 图理解为图片本身含按钮 → 用了 image map。第五轮回正：
  - 删 `assets/banner_v2.jpg`（扁平矢量 banner）。
  - 新图 `assets/banner.jpg`（Pillow 缩到 1280 宽、JPEG 58%、降彩×0.82 / 降对比×0.92 / 略提亮 — 出"老站实拍 + 略过曝"感，103KB）。
  - HTML：`banner` 内只有 `<img>`，overlay div 居中浮三件套：`<h2>槐 安 庙</h2>` + `<p>祈福·还愿·纳吉</p>` + 两个 `<a class="btn btn-big">`（金/红），分别跳 `#/reserve` `#/repay`，是可点击的真按钮，不是图。
  - CSS：`.banner-img` 加 `max-height:380px; object-fit:cover; object-position:center 30%`（避免 banner 过高占满首屏，同时保留天空+殿顶区域让标题浮在上面）。`.banner-overlay` 把上下都加深（背景 55%/25%/55%），金色标题在亮天空也能看清；副标加 text-shadow；按钮加深色投影浮起。
- 这一版才是第一阶段"正常老站"的正确 Banner：底图是真实老庙照片 + 浮 HTML 元素。后面剧情阶段可以靠改 banner 区的 JS 让浮层文案/按钮异常。
- 注意：环境无 HTML 截图，验收仍需用户浏览器实看 —— 特别检查标题/副标在亮天空区是否够清楚，必要时可再提高 overlay 顶部蒙版 alpha。

## 离线分享：打包成单个自包含 HTML
- 用户要"分享链接 + 不连网也能看"。当前站点本来就是纯静态（CSS/JS/相对路径图片），file:// 直接打开即离线；但要"发给别人容易"，做成单文件最稳。
- 脚本 `C:\Users\sh\AppData\Local\Temp\bundle_offline.py`（Pillow venv 运行）：读 index.html/css/style.css/js/app.js → 把 `<link>`→`<style>`、`<script src>`→`<script>` 内联 → 全局正则 `(?:\.\./)?assets/[...]` 把所有图片（含 CSS 的 `../assets` 和 JS config 的 `'assets/...'` 字符串两种写法）替换成 base64 `data:` URI → 输出 `huai-an-miao-offline.html`（~211KB）。
- 校验：12 个 data:image、1 个 <style>、HUAIAN.config 在内、外部 http(s)=0、file://=0。
- 关键坑：CSS 里图片是 `url(../assets/x)`（无引号），JS config 里是 `'assets/x'`（字符串），定点替换会漏，必须用全局正则无视引号统一替换。
- 流程可复用到任何"静态站转单文件离线包"的需求。

## 在线部署（CloudStudio 静态托管）
- 用户要"互联网网址链接 + 不下载 + 自动更新"。用 `workbuddy_cloudstudio_deploy` 部署项目根目录（含 index.html/css/js/assets），entry=index.html，port=3000。
- 返回的公开网址：`https://bd2d2add27c546d78cebce390bce61a2.sh5.agentos-app.net`（sandboxId: bd2d2add27c546d78cebce390bce61a2，verified=true）。
- 已 curl 验证：首页 200 且含"槐安庙/在线预约/我要还愿/banner-img"；/css/style.css、/js/app.js、/assets/banner.jpg、/assets/shop/01.jpg 均 200。
- 更新机制：静态站是快照式。用户改完源码后说"重新部署"，我重跑同一条 deploy 命令推最新版到同一网址（URL 不变，刷新即新内容）。不是 git push 式的真·自动同步（相关 connector 未连接）。
- 离线单文件（huai-an-miao-offline.html）与在线站是两份独立成品，改源码后两边都要各自重生成/重部署。

## 视觉修订（第四轮 · 同日）：回滚第三轮按钮照片 + 用用户提供的新 banner 图替换首图  ← 已被第五轮整段回滚
- 回滚：移除 `.btn-photo` / `.btn-photo-cap` 两个 CSS 规则；首页「在线预约」「还愿服务」按钮恢复普通土金样式；删除中间产物 `assets/banner/btn_temple.jpg`。
- 新 banner：把用户提供的 PNG（704×282，含"槐安庙"标题、"祈福·还愿·纳吉"副标、金/红按钮、底部"槐安庙实景·摄于己丑…"）保存为 `assets/banner_v2.jpg`（27KB，JPEG 78% 质量，降对比×0.95）。
- HTML：banner 区去掉原来的「overlay 文字+按钮」层，只剩 `<img usemap>`；用 `<map name="banner-map">` 加两个 `<area>`（25%,67%,49%,86% / 55%,67%,79%,86%）让图片里那两枚金/红按钮仍可点击，分别跳 `#/reserve` 和 `#/repay`。
- 像素坐标通过 Pillow+scipy.ndimage 连通组件算法自动测出（金按钮 x=176-345 y=190-243，红按钮 x=385-554 y=190-243）。
- 旧 `assets/banner.jpg` 也删了，不留垃圾。
- 验证：JS `node --check` 通过；所有引用的 assets 都存在；image map 百分比在现代浏览器均支持。

## 视觉修订（第三轮 · 同日）：把用户提供的实拍庙宇照作按钮底图
- 处理：Pillow 把原 461KB JPG 缩到 ~520px + JPEG 42% 质量 + 减彩/降对比，输出 `assets/banner/btn_temple.jpg`（13.6KB）。
- 应用：仅首页「在线预约」「还愿服务」两个按钮加 `.btn-photo` 类 —— 背景换成这张实拍图，加底部黑色蒙版让字可读；按钮内嵌左下角 `<span class="btn-photo-cap">` 小字说明（"摄于庚子年秋　游客李先生" / "摄于丙午年清明　本庙存档"），模拟老站照片 exif 风。
- 其他按钮未改动（继续维持土金按钮）。
- （本轮已被第四轮回滚。）

## 视觉修订（同日第二轮）：从「复古主题」改为「2008 遗风老站」
- 目标：让玩家觉得“这站 2005–2012 就上线、一直打补丁”，而非“2026 设计师做的复古站”。
- 字体：正文/标题统一 SimSun/宋体，正文 13px、辅助 11–12px、大标题 22px，去掉现代无衬线。
- 按钮：矩形立体（上左亮/下右暗 bevel），去掉圆角胶囊与柔和阴影；改用硬阴影(0 blur)。
- 素材：用 Python(Pillow) 生成真实低分辨率 JPEG/GIF，制造 JPEG 压缩/过曝/失真感：
  - `assets/banner.jpg`（4KB，粗糙庙宇实拍感，图内嵌“摄于己丑年修缮后”）
  - `assets/shop/01..08.jpg`（<1.1KB，尺寸刻意不一致 70–88px）
  - `assets/bg_tile.jpg`（48px 低分辨率平铺底纹）、`assets/dec/divider.jpg`（花纹分隔线）、`assets/dec/new.gif`（闪烁“新”标）
- 结构：Banner 改 `<img>` 低清图；新增「站内公告」面板 + 花纹分隔线；页脚补「网站地图/管理员信箱/IE与1024×768提示/未经许可不得转载」。
- 金色调降为土金(#b8902f)，红为砖红(#a81616)，去掉奢华渐变与发光阴影。
- Pillow 运行于隔离 venv：`C:/Users/sh/.workbuddy/binaries/python/envs/default`（PIL 12.3.0）。生成脚本在系统 temp，未入库。
- 注意：环境无 HTML 截图工具，未做像素级验收；建议用户在浏览器实际打开核对“老站感”是否到位。

## 在线部署（CloudStudio 静态托管）
- 网址：`https://bd2d2add27c546d78cebce390bce61a2.sh5.agentos-app.net`（sandboxId: bd2d2add27c546d78cebce390bce61a2）。已 curl 验证首页+子资源均 200。改源码后用户说"重新部署"即重推；URL 是否随重部署变化未知（工具描述说每次新建 workspace），所以不要静默重部署以免破坏已分享的 URL。

## 离线单文件 & 界面微调（同日）
- 离线包脚本：`C:\Users\sh\AppData\Local\Temp\bundle_offline.py` → 输出 `huai-an-miao-offline.html`（CSS/JS/图片全内联 base64，离线可双击打开）。全局正则替换 `(?:\.\./)?assets/...` 是关键（CSS 用 `url(../assets/x)`、JS config 用 `'assets/x'` 两种写法都要覆盖）。
- 界面微调：全站字体统一为 `--font-site:"SimSun","宋体","NSimSun",serif`（在 html/body/表单控件上强制）；链接 `--link:#0000ee` 蓝+下划线，新增 `--link-visited:#551a8b` 紫（加 `a:visited`）；底部友情链接 `.friend-links a` 由金色改回蓝。顶部导航 `.nav-link` 与祈福标签 `.pray-tag` 属控件类链接，不改成蓝下划线。

## 视觉修订（第五轮 · 跨日）：按用户提供的"地方旅游/民俗庙宇三栏红金模板"整体重排
- 用户的图（Clipboard 03-xx）是经典三栏地方景区官网：顶部细条 + 红底头部(牌匾+装饰图) + 红底导航 + 三栏正文 + 红底页脚。
- HTML 改动：新增 `.topbar`（设首页/收藏/天气访客数细条）；`site-header` 包 `.header-in` 且右侧加 `assets/dec/header_ornament.jpg`（福字香炉祥云）；`main-nav` 包 `.main-nav-in`；`view-home` 整段改为 `.home-cols`（`.col-left` 站内公告+快速导航+友情链接小图 / `.col-center` banner+6功能卡+今日祈福+庙宇简介 / `.col-right` 快速预约+还愿入口+精彩活动(fahui)+周边推荐(shop2三图)+在线客服）；`.site-footer` 限宽 1000px 居中。
- CSS 整体重写（382→约 470 行，144/144 括号平衡）：引入三栏 flex 布局；面板统一为"奶白底+金边+红标题条"；头部/导航/页脚走红金；body 底改旧米黄 `#f0e2c4`（不再用暗红 tile 铺满）；保留全部 JS 依赖的 ID/类名不变（view-home、notice-list、home-pray-tags、home-intro、reserve-*、repay-*、pray-*、history-timeline、shop-grid、cs-*、visit-count、modal-root 等）。
- 生成缺失模板素材：`gen_template_assets.py` 原本定义了 make_header_ornament/make_banner_main/make_fahui_card/make_intro_thumb 但**没调用**，脚本末尾补了调用才真正出图。新图：`assets/dec/header_ornament.jpg`(2.9KB)、`assets/banner_main.jpg`(10.8KB，未直接用在首页，首页仍用用户实拍 `assets/banner.jpg`)、`assets/dec/fahui.jpg`(3.7KB)、`assets/dec/intro_thumb.jpg`(0.8KB)。`assets/friend/f00-f05.jpg` + `assets/shop2/{amulet,bracelet,sachet}.jpg` 由脚本 products/friends 循环生成。
- 验证：JS `node --check` 通过；CSS 括号平衡；`grep -rhoE assets/...` 全 24 个引用文件均存在；所有 JS 依赖 ID 存在。环境无 HTML 截图，未做像素级验收。
- 待办：线上 URL 与离线单文件均未同步，需用户说"重新部署"/"重新打包"。
