# 2026-08-18

## 内页排版回滚 / 修复"日期点不动"
- 现象：三栏红金模板重设计（昨）后，预约/还愿页的日期格点不动、时段格无样式。
- 根因：重设计重写 `css/style.css` 时，漏掉了内页 JS 依赖的几个类——`.date-grid/.date-cell(+.full/.selected/.date-day/.date-status/.st-*)`、`.slot-grid/.slot-cell(+.full/.selected/.slot-time/.slot-left)`、预约成功弹窗的 `.qr-box/.qr-num`。JS 点击逻辑一直都在，只是元素无边框/`cursor:pointer`/选中高亮，看起来"点不动"。
- 修复：把这些类按旧版红金 bevel 风格补回 `css/style.css`（插在 `.form-actions` 之后）。内页 HTML 结构未动，首页三栏模板未动。
- 校验：CSS 括号 163/163 平衡；JS `node --check` 通过；7 个关键类均在。
- 重新部署：今日已用 `workbuddy_cloudstudio_deploy`（action=deploy）把最新源码推到同一网址 `https://bd2d2add27c546d78cebce390bce61a2.sh5.agentos-app.net`。curl 实测：HTTP 200，线上 `css/style.css` 已含 `.date-grid/.date-cell/.slot-grid/.qr-box`，首页 `topbar/home-funcs/banner-overlay` 与 3 张模板素材均 200。别人刷新即可看到内页日期可点 + 三栏模板。
- 离线单文件 `huai-an-miao-offline.html` 仍未更新，需"重新打包"才同步。

## 内容体系扩展：香客资料 / 还愿记录 / 祈福故事（隐藏主线）
- 目标：不打断现有结构/视觉/功能，新增可探索的"真实庙宇资料"内容，前 ~70% 正常，逐渐埋入"网站由林女士为完成『传名』还愿而建立"的隐藏真相。禁止任何元叙事/恐怖直白词。
- 新增 4 个视图（hash 路由）：`#/record` 历年还愿记录、`#/story` 香客故事、`#/review` 香客评价、`#/about` 关于本站。主导航加「历年还愿/香客故事/香客评价」三项（`.main-nav-in` 改 `flex-wrap` 容 10 项）。
- `js/app.js` config 新增：`records`(14条，林女士 r08/r12/r14 三条，r14 日期 2026-12-03 为未来、缺失 2023 年)、`stories`(13条，温和为主+4条轻微异常+林女士"搭网站"条+#2/#13 相似措辞)、`reviews`(12条，五星正常为主+「别去」「有些事情还是不要问」「这里以前不是这样的(林女士)」+已删除/注销/无法显示)、`repayWays`(常规香火供果… + 传名/引荐有缘人/留名/广而告之)、`aboutText`(暗示由林姓香客参与搭建)、`todayBase:127`。
- 还愿服务页加「还愿方式」面板(`#repay-ways`)；庙宇介绍页加「传名习俗」句；联系我们页加「引荐有缘人」句 + 「关于本站」入口。
- 渲染函数：`renderRecords`(表格+详情弹窗 showRecordDetail 模糊信息)/`renderStories`/`renderReviews`/`renderRepayWays`/`renderAbout`；记录详情点「详情»」弹窗，给模糊说明不透底。
- 第四面墙：`bumpToday`+`renderTodayVisits` 接在 `navigate` 的 pageView 后——每次浏览页面「今日访问人数」127 起 +1；`#today-visits-review` 在浏览≥3页后追加「今日新增香客：1」。新渲染函数已挂到 `window.HUAIAN`。
- 校验：JS `node --check` 通过；CSS 括号 189/189；12 个新 ID、4 条路由、8 个渲染函数均在。无新增图片资源。
- 未同步：线上站/离线单文件仍旧版，需"重新部署"/"重新打包"。
